<div class="d-flex justify-content-between align-items-center">
  <h1><%= @costume.title %></h1>
  <div>
    <%= link_to "Edit Costume", edit_costume_path, method: :get, class: "btn btn-primary" %>
    <%= link_to costume_path(@costume), class: "btn btn-warning ml-1",
        method: :delete,
        data: { confirm: "Deleteing #{@costume.title}.\nAre you sure?" } do %>
          <i class="far fa-trash-alt"></i>
        <% end %>
    <%= link_to 'Back', costumes_path, class: "btn btn-info ml-1" %>
  </div>
</div>
<p><%= @costume.description %></p>
<p><em>$<%= @costume.price %></em> per day</p>
<hr>
<div class="d-flex justify-content-start align-items-start flex-wrap  ">
  <% if @costume.photos.attached? %>
    <% @costume.photos.each do |pic| %>
      <img class="m-1" alt="<%= @costume.title %>" src="<%= cl_image_path pic.key %>"
          style="height: 200px; width: 200px; object-fit: cover; box-shadow: 0px 0px 4px darkgrey" />
    <% end %>
  <% else %>
    <br><h4>No photos attached.</h4>
  <% end %>
</div>
